https://discord.com/api/webhooks/1491805303975448696/vQf8856EYYwmAaBUYIB5wdygG60KnFuGA5YbqqejaX9--4sPjimKYohptvd2hQHwLILvname: Daily Trade Signal Bot

on:
  schedule:
    - cron: '5 0 * * *'
    - cron: '5 12 * * *'
    - cron: '5 18 * * *'
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Python Script
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python main.py
